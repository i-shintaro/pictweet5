    <%# modelオプションで指定した@tweetには（@tweet = Tweet.new）が入っており投稿先のパスやHTTPメソッドを自動で設定してくれる %>
<%= form_with(model: tweet, local: true) do |form| %>
  <%= form.text_field :image, placeholder: "Image Url" %>
  <%= form.text_area :text, placeholder: "text", rows: "10" %>
  <%= form.submit "SEND"%>
<% end %>
